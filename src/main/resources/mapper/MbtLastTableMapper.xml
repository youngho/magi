<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.famos.bets.dao.MbtLastTableDao">
    <select id="retrieveMbtLastTable" parameterType="String" resultType="kr.famos.bets.domain.MbtLastTable">
      SELECT * FROM MBT_LAST_TABLE WHERE ROWNUM = 1
    </select>
</mapper>
